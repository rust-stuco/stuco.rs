name: Deploy website

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: website-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Taplo
        uses: taiki-e/install-action@v2
        with:
          tool: taplo
          fallback: none

      - name: Validate TOML formatting
        run: taplo fmt --check resources/*.toml schedule/*.toml Cargo.toml

      - name: Validate TOML against schemas
        run: taplo check

  build:
    needs: validate
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event_name == 'pull_request' && format('preview-pr-{0}', github.event.pull_request.number) || 'production' }}
      url: >-
        ${{
          github.event_name == 'pull_request' &&
          format(
            'https://pr-{0}-stuco-rs.rust-stuco.workers.dev',
            github.event.pull_request.number
          ) ||
          steps.production.outputs.deployment-url
        }}
      deployment: >-
        ${{
          github.ref == 'refs/heads/main' ||
          (
            github.event_name == 'pull_request' &&
            github.event.pull_request.head.repo.full_name == github.repository
          )
        }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache bun packages
        uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-marp-cli

      # Needed to build the lecture slides
      - name: Install marp-cli
        run: bun install -g @marp-team/marp-cli

      # `build.rs` installs the pinned Slidev toolchain to render the week-one deck.
      - name: Cache npm packages
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-slidev-${{ hashFiles('slidev/package-lock.json') }}

      # Exporting the week-one PDFs renders in a browser. Point Slidev at the one the runner already
      # has, so the build does not download Playwright's copy as well.
      - name: Locate the browser for slide export
        run: |
          echo "STUCO_SLIDEV_CHROME=$(command -v google-chrome || command -v chromium)" \
            >> "$GITHUB_ENV"

      - name: Add wasm target
        run: rustup target add wasm32-unknown-unknown

      # Needed for wasm-opt
      - name: Install binaryen
        uses: sigoden/install-binary@v1
        with:
          repo: WebAssembly/binaryen
          name: wasm-opt

      # Needed to compile the syllabus
      - name: Install typst
        uses: typst-community/setup-typst@v4

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
            !target/dx/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Dioxus CLI
        uses: taiki-e/install-action@v2
        with:
          tool: dioxus-cli@0.7.2

      - name: Build
        env:
          # Limit parallel rendering to avoid exhausting CI runner resources.
          RAYON_NUM_THREADS: 2
        run: dx build --release

      # The deck is a pre-built single-page app, so it is built outside `public/`: `dx` re-bundles
      # every JavaScript file it finds there, which does not survive Slidev's module graph. Overlay
      # it onto the bundle that `wrangler` uploads instead.
      - name: Add the Slidev deck to the bundle
        run: |
          mkdir -p target/dx/stuco-rs/release/web/public/slidev
          cp -r target/slidev/week01 target/dx/stuco-rs/release/web/public/slidev/
          test -s target/dx/stuco-rs/release/web/public/slidev/week01/index.html

      - name: Upload syllabus artifact
        uses: actions/upload-artifact@v4
        with:
          name: syllabus
          path: target/dx/stuco-rs/release/web/public/syllabus.pdf

      - name: Deploy production website
        id: production
        if: github.ref == 'refs/heads/main'
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4"
          command: deploy

      - name: Deploy preview website
        id: preview
        if: >-
          github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name == github.repository
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4"
          command: versions upload --preview-alias pr-${{ github.event.pull_request.number }}
